image: docker:stable

options:
  docker: true

pipelines:
  default:
    - step:
        script:
          - continuous_integration/dependencies.sh
          - docker-compose up -d
          - docker-compose down
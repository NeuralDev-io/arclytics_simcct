apiVersion: v1
kind: PersistentVolume
metadata:
    name: redis-pv
    labels:
      type: local
spec:
  capacity:
    storage: 1.5Gi
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/redis-pv"

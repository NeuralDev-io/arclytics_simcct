apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: arc-gce-ingress
  namespace: arclytics
  annotations:
    kubernetes.io/ingress.global-static-ip-name: "arclytics-ip"
    kubernetes.io/ingress.class: "gce"
    # kubernetes.io/ingress.allow-http: "false"
spec:
  tls:
    - secretName: client-app-https-secret
    - secretName: simcct-app-https-secret
    - secretName: arclytics-app-https-secret
    - secretName: website-https-secret
#    - secretName: apm-app-https-secret
  backend:
    serviceName: client-service
    servicePort: 8080
  rules:
  - host: api.arclytics.io
    http:
      paths:
        - backend:
            serviceName: simcct-service
            servicePort: 8000
  - host: data.arclytics.io
    http:
      paths:
        - backend:
            serviceName: arclytics-service
            servicePort: 8001
  - host: app.arclytics.io
    http:
      paths:
        - backend:
            serviceName: client-service
            servicePort: 8080
#  - host: apm.arclytics.io
#    http:
#      paths:
#        - backend:
#            serviceName: apm-service
#            servicePort: 8200
#  - host: arclytics.io
#    http:
#      paths:
#        - backend:
#            serviceName: web-service
#            servicePort: 8080
